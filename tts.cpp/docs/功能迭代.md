2025年-12月-23日：中文数字归一化补充时间“HH:MM”读作“点”，并在数字后识别 %/％ 读作“百分之”，避免读音缺失。
2025年-12月-23日：放宽 scripts/verify_audio.py 默认阈值（SNR/RMSE/MAE/MaxDiff），降低相近音频的误报。
2025年-12月-23日：补充 AGENTS 指引，修改推理流程/模型结构后需用 scripts/verify_audio.py 验证音质。
2025年-12月-23日：新增音频回归验证脚本 scripts/verify_audio.py，用于对比基准与生成音频差异。
2025年-12月-23日：中文前端支持数字/单字母变量的加减乘除等于读法，并在数字间识别 x 读作“乘”。
2025年-12月-23日：中文数字归一化在数字间识别 +/= 并读作“加/等于”。
2025年-12月-23日：tts-cli 分词调试输出为词内点号与 @ 补充音素括号，避免读音缺失。
2025年-12月-23日：tts-cli 分词调试输出合并连续英文为词级括号，标点不加括号。
2025年-12月-23日：tts-cli 分词调试输出跳过英文/数字逐字括号，英文段更清爽。
2025年-12月-23日：Kokoro CPU 新增 Q8_0 权重量化（默认启用，TTS_CPU_CONV_Q8=0 可关闭），并提供可选的 conv 版 STFT/ISTFT（TTS_CPU_GRAPH_STFT/ISTFT，默认关闭）；小规模非 2 幂 FFT 增加 DFT 预计算以减少 trig 开销，同时将 tts-cli 默认线程数提升为硬件并发数。
2025年-12月-23日：'@' 符号默认读作“艾特/at”，覆盖独立符号与邮箱/提及场景。
2025年-12月-23日：邮箱/账号中的 '@' 中文读作“艾特”，并在 tts-cli 分词调试输出中为非空白边界符补充括号标注。
2025年-12月-23日：中文多点号数字读法优化（如 1.2.3 → 一点二点三），并收紧“词内点号”规则：中文词间 `.` 不再读作“点”。
2025年-12月-23日：多语前端补充“词内点号”处理，中文读作“点”、英文读作“dot”，不影响正常句号。
2025年-12月-23日：中文单位归一化增强：补齐 `s/ms/us/ns/min/h` 等时间单位缩写，并支持 `m/s`、`km/h`、`mg/L` 这类斜杠单位读作“米每秒/公里每小时/毫克每升”（`/`→“每”，仅在分子/分母都能识别为单位时触发以避免误伤 URL/路径）。

2025年-12月-23日：新增 GitHub Actions Nightly 自动构建发布：Windows（MSVC/MinGW64）与 Ubuntu20.04（x86_64/aarch64）四套环境，均提供 CPU/Vulkan 两种 `tts-cli` 产物，并在每次 push 后自动覆盖更新 nightly release。

2025年-12月-23日：中文数字归一化增强：识别 `YYYY年`（4 位数字 + “年”）并按逐位读（如 `2025年`→`二零二五年`），避免被当作普通整数读成“二千零二十五年”。

2025年-12月-23日：Kokoro 中文前端“短语词典优先”：当短语词典为该词提供完整拼音序列（含声调）时，跳过自动变调/轻声（如三声连读、叠词轻声、“一/不”变调等）以完全遵循词典标注；tts-cli `--help` 改为简洁版并新增 `--help-all` 查看完整参数，同时移除已弃用的 `--use-vulkan/--use-metal/--vulkan-device`。

2025年-12月-23日：修复 Kokoro 中文 DP 分词在最小代价相同时偏向短短语（如“好好学习”未命中长词条）的问题，改为 tie-break 优先更长短语；同时增强 tts-cli 默认中文词典路径自动查找：除 `./dict` 外支持从 exe_dir 向上搜索 `dict/` 并在日志中打印实际命中路径。

2025年-12月-22日：新增 tts_conv_1d 封装支持 F32 卷积核，替换 ggml_conv_1d 调用以修复未量化权重在 CPU 上触发断言的问题。

2025年-12月-22日：CPU-only 推理绕开 ggml scheduler（避免其按 graph_size 申请超大内部内存池），改用 `ggml_gallocr` 按实际图规模分配/复用计算 buffer，修复 ARM Linux 无 swap/严格 overcommit 下加载阶段触发 `ggml_aligned_malloc` OOM 并断言崩溃；同时统一各模型 runner 通过 `runner_context` 封装完成 reset/alloc/compute。

2025年-12月-21日：新增 CosyVoice3 GGUF 转换脚本与编码器，覆盖 llm/flow/hift 权重映射与 weight_norm 折叠，写入 Qwen2 BPE 词表 + CosyVoice3 special tokens，并支持预制 voice pack 写入与固定噪声/Hann 窗常量。

2025年-12月-21日：新增 `docs/CosyVoice3_CPU_推理待办清单.md`，规划 CosyVoice3 预制 voice pack + CPU 非流式推理 + 单文件 GGUF 的实施步骤，并明确 Qwen2 结构将参考 `参考项目/llama.cpp`。

2025年-12月-21日：Kokoro 多语言（含中文/注音）长文本判断改为按 UTF-8 字符数（token 数）而非字节数，避免中文场景被误判为超长导致不必要的 chunking/多次推理，显著降低 Vulkan 下的重复图构建与输出回读开销。

2025年-12月-21日：修复 Vulkan 下 Kokoro 生成音频出现“沙沙声/电流声”：`stft_graph` 在计算 magnitude/angle 时改为基于 reshape 后的连续布局进行逐元素运算，最后再 `permute` 到目标布局，避免 Vulkan 对 permute/view 大 stride 输入的逐元素算子兼容性差异导致相位抖动与音质劣化。

2025年-12月-21日：tts-cli 默认打印“原文/音素串/分词(词(音素))”（输出到 stderr），便于快速排查多音字、数字/单位归一化与卷舌等细节；Kokoro runner 增加 `try_phonemize/try_phonemize_segments` 调试接口；中文前端新增 debug API 输出“分词对齐”。

2025年-12月-21日：新增 `docs/Kokoro_CPU_推理性能优化代办清单.md`，整理 Kokoro CPU 侧不依赖硬件/第三方库的结构与流程优化方案，并给出参考 whisper.cpp 的可执行代办清单。

2025年-12月-21日：修复中文“卷舌/舌尖元音”兜底：内置单字拼音表补齐 i→ii/iii（zi/ci/si 与 zhi/chi/shi/ri）区分，避免单字/短词场景卷舌音不明显；数字归一化增强：支持小数点前后存在空格的写法（如 `23 . 5` / `23. 5`）。

2025年-12月-21日：Kokoro 中文体验增强：修复小数点读法（分隔符后首位数字被跳过导致小数部分丢失）；多语言前端新增 °C/℃/°F/℉ 与常见单位缩写的中文归一化；tts-cli 在未指定 `--zh-dict-dir` 且工作目录无 `dict/` 时自动尝试 exe 同目录 `dict/`；Kokoro 标点断句优化（`.!?` 改为弱停顿并在长文本 chunking 句块间补逗号）；新增 `tts-zh-frontend` 调试工具用于输出前端 token 串。

2025年-12月-21日：tts-cli 新增 `--bench` 内置中英语音测试文本，指定后自动推理并忽略 `-p/--prompt`。

2025年-12月-21日：Kokoro Vulkan “金属音”排查/兜底：新增 STFT/ISTFT 的 Vulkan 质量开关（`TTS_VK_GRAPH_STFT=0` / `TTS_VK_GRAPH_ISTFT=0` 回退到自定义算子），并支持仅将基矩阵相关的 STFT/ISTFT 卷积固定到 CPU（`TTS_VK_STFT_CPU=1` / `TTS_VK_ISTFT_CPU=1` / `TTS_VK_AUDIO_QUALITY=1`）以尽量保留其余 Vulkan 加速。
2025年-12月-20日：修复 Kokoro Vulkan 生成“金属音”：Snake1d 改为直接计算 `sin(alpha*a)^2 / alpha`，并调整 reciprocal 的实现以避免 Vulkan 下对标量常量做 repeat 广播引发的数值/后端边界问题，尽量保持加速同时恢复音质。
2025年-12月-20日：Kokoro CPU 推理优化：LSTM gate 权重融合（I/F/G/O 合并为单次 mul_mat）+ ggml_set_2d_inplace 预分配输出替代逐步 concat（消除 O(T^2) 拷贝）；duration hidden_states 改为按节点名查找避免固定偏移失效（可用 `TTS_CPU_LSTM_FUSE=0` / `TTS_CPU_LSTM_SET_OUTPUT=0` 回退）。
2025年-12月-20日：CPU-only 预分配增加阈值保护与开关（`TTS_CPU_PREALLOC` / `TTS_CPU_PREALLOC_MAX_GIB`），避免 worst-case 图触发数 GiB 申请导致启动变慢/不稳定。
2025年-12月-20日：Kokoro CPU 推理优化：duration 阶段默认限制线程数到 4（`TTS_CPU_THREADS_DURATION` 可调），并优化 STFT/ISTFT 自定义算子（FFT twiddle 预计算、one-sided 直接输出、ISTFT 镜像频点补齐减半 trig）。
2025年-12月-20日：tts-cli 新增 `--device` 统一选择 cpu/metal/vulkan 等，默认跟随编译启用的后端（启用 Vulkan 则默认 Vulkan）；旧参数 `--use-vulkan/--use-metal` 标记弃用但兼容，保留 `--vulkan-device`。
2025年-12月-20日：Vulkan 下默认跳过“最坏图”预分配，并在预分配前增加后端单 buffer 上限检查，避免启动阶段大额申请/失败导致耗时。
2025年-12月-20日：Kokoro 生成图用 duration_ids(I32)+get_rows 替代 duration_mask+mul_mat 展开，减少节点与内存占用并提升 CPU/Vulkan 推理速度。

2025年-12月-20日：tts-cli 默认线程数改为硬件并发的一半（最少 1），降低超线程下的调度/缓存竞争开销。

2025年-12月-20日：Kokoro 生成器残差块改为时间优先布局广播 gamma/beta，减少 transpose/cont，降低 Vulkan 下 IM2COL/CONT 开销。
2025年-12月-20日：新增 Vulkan 开发指南文档，并补齐 Vulkan 图常量输入登记/写入机制，避免常量张量访问 host 指针导致崩溃。
2025年-12月-20日：将工程与 Kokoro/tts-cli 等核心代码适配到 C++17（替换 contains/starts_with/ends_with/指定初始化等 C++20 用法），并修复 numbers_compat/ggml-iterator 在 C++17 下的兼容问题，以兼容低版本 Linux 编译器。
2025年-12月-20日：Kokoro 生成阶段 STFT/ISTFT 改为 conv 基矩阵实现，新增 atan2 近似与反射 padding 索引输入，避免自定义算子并完善 Vulkan 完整支持。
2025年-12月-20日：Kokoro 新增日文前端（轻量版）：支持假名（平/片假名）到音素字符序列的转换，并在 Kokoro 多语言分流中识别日文假名；CLI `--lang` 扩展支持 `ja`（用于日文前端/数字处理偏好）。支持“汉字(かな)”形式注入读音（不引入 OpenJTalk/MeCab，汉字自动读音仍有限）。
2025年-12月-20日：Kokoro 中文拼音词典支持“内置编译进二进制”（CMake 选项 `TTS_KOKORO_ZH_DICT_BUILTIN`，默认 ON），运行时 `--zh-dict-dir` 为空会先尝试 `./dict`，失败则回退内置；支持 `:builtin` 强制内置，`-` 禁用词典路径。
2025年-12月-20日：Kokoro 中文前端集成短语拼音词典（pinyin_phrase/pinyin）做多音字消歧，并补齐轻声/儿化/更完整变调；生成配置新增 `zh_dict_dir`，CLI 新增 `--zh-dict-dir/-zd`（默认尝试 `./dict`，传 `-` 可禁用）。
2025年-12月-19日：Kokoro 中文数字读法新增“>5 位逐位读”规则，并在 CLI 增加 `--lang` 控制数字中文/英文读法（默认中文）。
2025年-12月-19日：Kokoro Vulkan 下 depthwise pool 展开权重时自动切换 groups==1 路径；生成阶段仅强制 matmul/conv_transpose 走 Vulkan，并支持失败回退 CPU；新增 TTS_VK_HOST_BUFFER 控制主机可见权重；Vulkan 图构建增加非 Vulkan buffer 检测以避免直接崩溃。
2025年-12月-19日：中文前端增加数字转中文读法（支持负号/小数/全角数字），避免数字被当作声调数字。
2025年-12月-19日：调整 Kokoro 多语言分段：数字默认交由中文前端处理，避免与英文片段混杂。
2025年-12月-19日：Kokoro 在 Vulkan 下为 conv_transpose_1d 生成 F32 权重副本，并将 depthwise pool 转置卷积权重展开为全通道形式，减少 CPU 自定义算子占比。
2025年-12月-19日：Kokoro 生成图在 Vulkan 检测到自定义算子时，额外强制可支持的非自定义节点走 Vulkan，避免整图被 CPU anchor。
2025年-12月-19日：Kokoro 生成阶段改为“自定义算子仅固定 CPU、其余仍走 Vulkan”，避免整图回退导致 GPU 无法参与计算。
2025年-12月-19日：Kokoro 的 uv/noise 生成从自定义 map 改为标准 ggml 算子组合，减少 Vulkan 图自定义算子数量。
2025年-12月-19日：Kokoro 在 Vulkan 下默认改用主机可见权重缓冲，避免自定义算子回退 CPU 时读取设备内存导致崩溃，可用 TTS_VK_FORCE_GEN=1 跳过。
2025年-12月-19日：Vulkan 显存信息不可用时跳过预分配并在失败后重置调度器；Kokoro 时长/生成图分配失败时回退 CPU 且校验计算状态，避免崩溃。
2025年-12月-19日：各推理流程在异步后端完成计算后先同步，再 reset 调度器，避免 Vulkan/Metal 下释放仍在使用的 buffer 导致崩溃或输出未就绪。
2025年-12月-19日：新增 perf_battery 基准测试工具，内置中英混合标准用例并支持 backend/repeat/warmup 等简化参数，输出统一耗时与 RTF 汇总。
2025年-12月-19日：修复 Kokoro 内部重复 ggml_time_init 导致 tts-cli 生成耗时为负，并在 Windows 控制台强制使用 UTF-8 以避免中文日志乱码。
2025年-12月-19日：Vulkan 下 Kokoro 生成阶段检测到自定义算子时自动回退 CPU，并提供 TTS_VK_FORCE_GEN 强制开关以规避 ggml-vulkan 构图崩溃。
2025年-12月-19日：Vulkan 下 Kokoro 视图节点若依赖自定义算子则强制 CPU，规避构图混用 buffer 崩溃。
2025年-12月-19日：Vulkan 下 Kokoro 输入叶子强制绑定 Vulkan 后端，避免调度混用 CPU buffer 导致构图崩溃。
2025年-12月-19日：修复 Kokoro 运行结束时双重释放（model/kctx）导致 tts-bench 退出崩溃的问题，统一由 kokoro_runner 管理资源释放。
2025年-12月-19日：Vulkan 下 Kokoro 输入准备改用 backend 写入并缓存 Hann 窗函数，避免 CPU 直接访问 GPU 内存导致崩溃。
2025年-12月-19日：移除 eSpeak 相关构建与运行时逻辑，音素化统一使用内置 phonemizer + zh_frontend，并同步清理相关 CLI/文档/转换脚本。
2025年-12月-19日：Vulkan/Metal 预分配前增加显存评估，超出时跳过预分配以避免启动阶段 OOM。
2025年-12月-19日：修复 Vulkan 主机可见缓冲对齐使用 minStorageBufferOffsetAlignment，避免回退到主机内存时出现 buffer 偏移不对齐断言。
2025年-12月-19日：tts-cli 增加 Vulkan 后端支持：新增 `--use-vulkan/-vk` 与 `--vulkan-device/-vd` 参数，并在推理侧补齐 Vulkan/Metal 通用后端初始化与 buffer type 选择逻辑。
2025年-12月-19日：新增 `docs/gguf模型转换指南.md` 记录原始权重→GGUF 转换流程（Kokoro/Dia/Orpheus/Parler/T5），并修复 `py-gguf/convert_orpheus_to_gguf` 未传递 `--snac-repo-id` 导致参数不生效的问题。
2025年-12月-19日：验证 quantize 可正常量化 Kokoro（FP32→Q4_0）并修复 ggml 0.9.4 下需跳过 “GGUF tensor data binary blob”；新增 `docs/ggml升级指南.md` 记录 ggml 替换升级流程与常见兼容点。
2025年-12月-19日：修复 Kokoro v1.1 GGUF 加载/推理兼容：放宽 GGML_MAX_NAME 至 128 以支持 64 字符 tensor 名称，并在 Kokoro 图构建中补齐 ggml_cont 避免 ggml-cpu “非连续 src1 + broadcast” 断言。
2025年-12月-19日：升级 ggml 至 0.9.4；项目侧适配 gguf 拆分与 scheduler 新签名，补齐 STFT/ISTFT 与转置卷积兼容封装（padding/output_padding/groups），并修复 quantize 示例在新 gguf API 下的编译。
2025年-12月-19日：tts-cli 启动时增加关键阶段进度日志（参数解析/模型加载/生成/VAD/播放或写文件），避免长时间无输出造成“像没运行”的误解。
2025年-12月-18日：tts-bench 默认关闭库内部 timings（避免刷屏/影响计时），并在 README 补充 md 输出保存示例。
2025年-12月-18日：补全 tts-bench README 的参数说明（含 -r/-w/--threads 范围语法、输出格式与用例来源等）。
2025年-12月-18日：新增 tts-bench 基准测试工具（参考 llama-bench），支持多线程/多用例重复测试并输出 md/csv/json/jsonl，便于对比速度优化效果。
2025年-12月-18日：修复 MinGW 下静态初始化顺序不确定导致的 tts-cli 启动 SIGFPE 崩溃。
2025年-12月-18日：MinGW/Linux 默认静态链接 libstdc++/libgcc，并强制 OpenMP 使用静态 libgomp，减少运行时依赖。
2025年-12月-18日：Kokoro CPU 推理增加分段计时，并优化 duration predictor 注意力与输入准备开销。
2025年-12月-18日：优化 single_pass_tokenizer 以降低多语言/中文 prompt 的 tokenizer 固定开销，并修复 Kokoro 中 new/free 不匹配。
2025年-12月-22日：修复 NVIDIA Vulkan 下 STFT 图中 ABS/STEP 的 view 偏移未对齐断言，Vulkan 时对实/虚部 view 做 cont 对齐，并完善 LSTM/FFN 的 unary 对齐处理以避免回退 CPU 崩溃。
2025年-12月-22日：根因：Vulkan 下 STFT 图对实/虚部做 view 切片且 view_offs 未对齐，进入 GGML_OP_UNARY（ABS/STEP）触发 storage buffer offset 对齐断言；强制回退 CPU 又因 im2col 的 F16 约束导致类型断言。预防：Vulkan 路径对带偏移的 view 先做 cont 对齐，STFT 图保持连续布局并仅对严格对齐算子启用回退；后续新增 NVIDIA Vulkan 回归测试覆盖 STFT/UNARY 组合场景。









